= simple_form_for([:admin, @question_level], html: { class: 'form-horizontal' }) do |f|
  = f.error_notification
  .form-inputs
    = f.input :name, input_html: { class: 'span12' }
  .form-actions
    = f.button :submit, class: 'btn btn-primary'