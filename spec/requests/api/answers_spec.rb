require 'spec_helper'

describe "answers" do

  # let(:user) { create(:user) }
  describe "POST update" do


  end

  describe "GET show" do
    it "should get answer @answer" do
      # sign_in user
      # answer = create(:answer)
      # exam = create(:exam)
      # get "/api/answers/#{answer.id}?exam_id=#{exam.id}"
      # assigns(:answer).should eq answer
    end
  end
end